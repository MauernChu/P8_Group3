<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="p8.group3.ida.aau.p8_group3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginPage" android:screenOrientation="portrait">
            android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CreateAccountPage" android:screenOrientation="portrait">
            android:name=".CreateAccount">
        </activity>

        <activity
            android:name=".CreateAccountSecondPage" android:screenOrientation="portrait">
            android:name=".CreateAccount2">
        </activity>

        <activity
            android:name=".ProfilePage" android:screenOrientation="portrait">
            android:name=".ProfilePage">
        </activity>

    </application>

</manifest>
